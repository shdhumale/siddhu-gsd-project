---
phase: 1
plan: 2
wave: 2
depends_on: [1]
files_modified:
  - ticket-system-frontend/package.json
  - ticket-system-frontend/proxy.conf.json
  - ticket-system-frontend/src/app/services/ticket.service.ts
  - ticket-system-frontend/src/app/components/dashboard/dashboard.component.ts
  - ticket-system-frontend/src/app/components/dashboard/dashboard.component.html
  - ticket-system-frontend/src/app/app.config.ts
autonomous: true
---

# Plan 01-02: Angular Frontend & Dashboard

Establish the Angular application and the primary dashboard UI.

<tasks>
<task id="1" name="Initialize Angular Project">
<read_first>
- .planning/phases/01-foundation/01-CONTEXT.md (D-01)
</read_first>
<action>
Create a new Angular 17+ project in `/ticket-system-frontend` using Angular CLI.
Configure for:
- Standalone components (default).
- Routing enabled.
- SCSS for styling.
Add Angular Material: `ng add @angular/material` (Indigo/Pink theme).
</action>
<acceptance_criteria>
- Directory `ticket-system-frontend/` exists.
- `package.json` contains `@angular/material`.
- `ng serve` starts the app.
</acceptance_criteria>
</task>

<task id="2" name="Configure Dev Proxy">
<read_first>
- .planning/phases/01-foundation/01-RESEARCH.md
</read_first>
<action>
Create `proxy.conf.json` in the frontend root to route `/api` to `http://localhost:8080`.
Update `angular.json` or start script to use this proxy.
</action>
<acceptance_criteria>
- `proxy.conf.json` exists with correct mapping.
</acceptance_criteria>
</task>

<task id="3" name="Implement Ticket Service">
<read_first>
- .planning/REQUIREMENTS.md ([REQ-001])
</read_first>
<action>
Create `ticket.service.ts`.
Implement `getTickets()` method using `HttpClient` to fetch from `/api/tickets`.
Define `Ticket` interface matching the backend entity.
</action>
<acceptance_criteria>
- `TicketService` is implemented and injected.
</acceptance_criteria>
</task>

<task id="4" name="Build Dashboard & Table">
<read_first>
- .planning/phases/01-foundation/01-UI-SPEC.md
</read_first>
<action>
Crate `DashboardComponent`.
Implement `mat-table` to display tickets.
Columns: ID, Name, Description, Status.
Apply status badge styling as per UI-SPEC.
Add `mat-paginator` and `mat-sort`.
</action>
<acceptance_criteria>
- Dashboard renders a table.
- Table columns match UI-SPEC.
- Table displays data fetched from the service.
</acceptance_criteria>
</task>
</tasks>

<verification>
## Verification Criteria

1.  **UI:** `http://localhost:4200` loads the dashboard.
2.  **Integration:** Dashboard table displays the 5+ mock tickets seeded by the backend.
3.  **Styling:** Status badges use colors from the UI-SPEC (e.g., Blue for New, Amber for Assigned).
4.  **Responsiveness:** Table remains readable on standard desktop screens.
</verification>

<must_haves>
- [ ] Angular project initialized with Material.
- [ ] Functional dashboard component with data table.
- [ ] Successful data fetch from Spring Boot backend.
</must_haves>
