---
phase: 2
plan: 1
wave: 1
depends_on: []
files_modified:
  - ticket-system-frontend/src/app/services/ticket.service.ts
  - ticket-system-frontend/src/app/components/ticket-form/ticket-form.component.ts
  - ticket-system-frontend/src/app/components/ticket-form/ticket-form.component.html
  - ticket-system-frontend/src/app/components/ticket-form/ticket-form.component.scss
  - ticket-system-frontend/src/app/components/dashboard/dashboard.component.ts
  - ticket-system-frontend/src/app/components/dashboard/dashboard.component.html
autonomous: true
requirements: [REQ-002]
---

# Plan 02-01: Create Ticket Form

Implement the Create Ticket functionality with Angular Material Dialog and service integration.

<objective>
**Goal:** Allow users to create new tickets via an Angular Material dialog form.
**Purpose:** Per D-01 in CONTEXT.md — use Angular Material Dialog component as modal.
**Output:** Working ticket creation form connected to backend POST endpoint.
</objective>

<context>
@.planning/phases/02-operations/02-CONTEXT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

From ticket-system-frontend/src/app/services/ticket.service.ts:
```typescript
export interface Ticket {
  id: number;
  name: string;
  description: string;
  status: string;
}
```

From ticket-system-backend/src/main/java/com/example/ticketsystem/ticket/controller/TicketController.java:
```java
@PostMapping
@ResponseStatus(HttpStatus.CREATED)
public Ticket createTicket(@RequestBody Ticket ticket)
```

From .planning/REQUIREMENTS.md:
- REQ-002: Create Ticket — Form with Name, Description, Status (New, Assigned, Done, Escalate)
</context>

<tasks>

<task id="1" name="Extend TicketService with createTicket method">
<read_first>
- ticket-system-frontend/src/app/services/ticket.service.ts
</read_first>
<action>
Add the createTicket method to TicketService:

```typescript
createTicket(ticket: Omit<Ticket, 'id'>): Observable<Ticket> {
  return this.http.post<Ticket>(this.apiUrl, ticket);
}
```

Import HttpClientModule in app.config.ts if not already present.
</action>
<acceptance_criteria>
- ticket.service.ts contains createTicket() method
- Method returns Observable<Ticket>
- Uses POST to /api/tickets
</acceptance_criteria>
<done>TicketService extended with createTicket()</done>
</task>

<task id="2" name="Create Ticket Form Dialog Component">
<read_first>
- ticket-system-frontend/src/app/components/dashboard/dashboard.component.ts
- ticket-system-frontend/src/app/components/dashboard/dashboard.component.html
</read_first>
<action>
Create new component `ticket-form` in `src/app/components/ticket-form/`:

**ticket-form.component.ts:**
- Use Angular Material Dialog (MatDialog)
- Import: MatFormFieldModule, MatInputModule, MatSelectModule, MatButtonModule, ReactiveFormsModule, FormsModule
- Implement FormGroup with: name (required, max 100 chars), description (textarea, max 500 chars), status (required, dropdown)
- Status options: 'New', 'Assigned', 'Done', 'Escalate'
- Default status: 'New'

**ticket-form.component.html:**
- Form in dialog template with mat-form-field for each control
- Cancel and Save buttons in dialog-actions

**ticket-form.component.scss:**
- Set dialog width to 500px
- Ensure form-field fills width

Per D-01: Use Angular Material Dialog component, opens as modal over dashboard.
</action>
<acceptance_criteria>
- Component created at src/app/components/ticket-form/
- Contains name, description, status form controls
- ReactiveFormsModule properly configured
- Dialog opens from dashboard
</acceptance_criteria>
<done>Ticket form dialog component created</done>
</task>

<task id="3" name="Integrate Create Button into Dashboard">
<read_first>
- ticket-system-frontend/src/app/components/dashboard/dashboard.component.ts
- ticket-system-frontend/src/app/components/dashboard/dashboard.component.html
- ticket-system-frontend/src/app/components/dashboard/dashboard.component.scss
</read_first>
<action>
Update dashboard to include create functionality:

**dashboard.component.ts:**
- Import MatToolbarModule, MatButtonModule, MatIconModule
- Import MatDialog and TicketFormComponent
- Add openCreateDialog() method that opens dialog and subscribes to afterClosed
- After closed with data: call ticketService.createTicket(), then refresh table data

**dashboard.component.html:**
- Add toolbar above table with "Create Ticket" button (mat-raised-button with mat-icon)
- Add click handler openCreateDialog()

Per D-05: Create tab opens create dialog from button in dashboard toolbar.
</action>
<acceptance_criteria>
- Dashboard shows "Create Ticket" button
- Clicking button opens form dialog
- Submitting form creates ticket and refreshes table
- Error handling if create fails
</acceptance_criteria>
<done>Create button integrated into dashboard</done>
</task>

</tasks>

<verification>

## Verification Criteria

1.  **Service:** `npm run build` succeeds with new service methods
2.  **Component:** Form dialog opens from dashboard button
3.  **Validation:** Required fields enforced, invalid shows error
4.  **Create:** Submit saves to database via POST /api/tickets
5.  **Refresh:** Table updates after successful create

</verification>

<success_criteria>
- [x] User can click "Create Ticket" button
- [x] Modal dialog opens with form
- [x] Form validates Name, Description, Status
- [x] Submit saves to MySQL via API
- [x] Table refreshes to show new ticket
</success_criteria>

<output>
After completion, create `.planning/phases/02-operations/02-01-SUMMARY.md`
</output>